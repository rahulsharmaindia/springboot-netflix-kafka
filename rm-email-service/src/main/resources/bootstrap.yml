server:
  port: ${port:8082}
spring:
  application:
    name: rm-email-service
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: rm-config-server